## 1. Header UI

- [x] 1.1 Update the header component to accept a save callback and render a save icon button.
- [x] 1.2 Add a tooltip trigger in the header with copy about dragging to delete and importing full lists from the import tab.

## 2. Wiring and Validation

- [x] 2.1 Wire the new header save action from the screen that owns the header state.
- [x] 2.2 Verify the header still fits the current layout on small screens.
- [x] 2.3 Confirm the tooltip text is readable and matches the requested behavior.
