## 1. UI primitives

- [x] 1.1 Define the public API for `Input`, `TextArea`, `Dialog`, and `Tooltip` in `app/components/lib`
- [x] 1.2 Implement the `Input` component with the shared form styling
- [x] 1.3 Refine `TextArea` to match the shared input styling and multiline behavior
- [x] 1.4 Implement the `Dialog` component set for modal presentation and actions
- [x] 1.5 Implement the `Tooltip` component for contextual hints

## 2. App integration

- [x] 2.1 Replace any duplicated local text field styling with the shared `Input` or `TextArea`
- [x] 2.2 Add at least one screen-level usage of `Dialog` to validate the interaction pattern
- [x] 2.3 Add at least one screen-level usage of `Tooltip` to validate the interaction pattern

## 3. Verification

- [x] 3.1 Check component rendering and behavior on the target Expo platform(s)
- [x] 3.2 Verify the change does not break existing screens that use the shared UI layer
