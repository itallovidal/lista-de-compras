# Code Style

- Prefer named function declarations or named function expressions for components, screens, and reusable logic in `app/pages` and `app/components`.
- Keep arrow functions only for narrow local cases where they are clearly simpler or required.
- Inline button handlers are allowed when they stay short and local.
- Do not apply this rule to `app/lib` or `app/components/lib`.
